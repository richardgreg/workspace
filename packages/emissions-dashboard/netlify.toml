[[plugins]]
  package = "@netlify/plugin-nextjs"

[build]
  command = "yarn netlify-build"
  publish = "out"

[[plugins]]
  package = 'netlify-plugin-contextual-env'
  [plugins.inputs]
    mode = 'prefix'

